---
phase: 08-business-onboarding
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - supabase/migrations/YYYYMMDD_business_profiles.sql
  - src/types/onboarding.ts
  - src/lib/onboarding/schemas.ts
  - src/lib/onboarding/onboarding-dal.ts
  - src/lib/onboarding/prompts.ts
  - src/lib/ai/providers.ts
  - src/components/ui/popover.tsx
  - src/components/ui/command.tsx
autonomous: true

must_haves:
  truths:
    - "Tabela user_business_profiles istnieje w Supabase z RLS"
    - "DAL zwraca profil biznesowy zalogowanego użytkownika"
    - "DAL zapisuje profil i ustawia onboarding_completed=true"
    - "Schematy Zod walidują formularz i chat response"
    - "Komponenty Popover i Command są dostępne w src/components/ui/"
  artifacts:
    - path: "supabase/migrations/YYYYMMDD_business_profiles.sql"
      provides: "Tabela, trigger, index, RLS policies"
      contains: "CREATE TABLE user_business_profiles"
    - path: "src/types/onboarding.ts"
      provides: "TypeScript types for business profile"
      contains: "BusinessProfile"
    - path: "src/lib/onboarding/schemas.ts"
      provides: "Zod validation schemas"
      contains: "businessProfileSchema"
    - path: "src/lib/onboarding/onboarding-dal.ts"
      provides: "Server actions CRUD"
      exports: ["getBusinessProfile", "saveBusinessProfile"]
    - path: "src/lib/onboarding/prompts.ts"
      provides: "System prompt for onboarding chat"
      contains: "ONBOARDING_CHAT_SYSTEM_PROMPT"
    - path: "src/lib/ai/providers.ts"
      provides: "Onboarding model config"
      contains: "onboarding"
  key_links:
    - from: "src/lib/onboarding/onboarding-dal.ts"
      to: "src/lib/supabase/server"
      via: "createClient import"
      pattern: "createClient.*server"
    - from: "src/lib/onboarding/schemas.ts"
      to: "zod/v4"
      via: "Zod v4 import"
      pattern: "from.*zod/v4"
---

<objective>
Fundamenty modułu onboardingu biznesowego — baza danych, typy, schematy walidacji, DAL (server actions), prompt AI i komponenty shadcn/ui.

Purpose: Wszystkie kolejne plany (formularz, chat, banner, integracja) potrzebują tych artefaktów jako fundament.
Output: Tabela DB z RLS, typy TS, schematy Zod, DAL z CRUD, prompt chatu, konfiguracja modelu AI, komponenty Popover+Command.
</objective>

<execution_context>
@/Users/dariuszciesielski/.claude/get-shit-done/workflows/execute-plan.md
@/Users/dariuszciesielski/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-business-onboarding/08-RESEARCH.md

@src/lib/ai/providers.ts
@src/lib/gamification/gamification-dal.ts
@src/lib/focus/focus-dal.ts
@src/app/(dashboard)/profile/actions.ts
@src/types/database.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: DB migration + TypeScript types + Zod schemas + AI prompt + provider config</name>
  <files>
    supabase/migrations/YYYYMMDD_business_profiles.sql
    src/types/onboarding.ts
    src/lib/onboarding/schemas.ts
    src/lib/onboarding/prompts.ts
    src/lib/ai/providers.ts
  </files>
  <action>
1. Utwórz migrację SQL w `supabase/migrations/` (użyj aktualnej daty w nazwie, np. `20260308120000_business_profiles.sql`):
   - Tabela `user_business_profiles` z kolumnami: id (UUID PK), user_id (UUID UNIQUE, FK auth.users ON DELETE CASCADE), industry (TEXT NOT NULL), role (TEXT NOT NULL), business_goal (TEXT NOT NULL), company_size (TEXT nullable), experience_summary (TEXT nullable), onboarding_completed (BOOLEAN NOT NULL DEFAULT false), created_at (TIMESTAMPTZ DEFAULT now()), updated_at (TIMESTAMPTZ DEFAULT now())
   - Trigger `update_business_profile_timestamp` — auto-update updated_at (wzorzec z RESEARCH.md)
   - Index na user_id
   - RLS: ENABLE + 3 policies (SELECT, INSERT, UPDATE) z `auth.uid() = user_id`
   - COMMENT ON TABLE

2. Utwórz `src/types/onboarding.ts`:
   - Interface `BusinessProfile` (wszystkie kolumny tabeli)
   - Type `BusinessProfileInput` (industry, role, business_goal, company_size?)

3. Utwórz `src/lib/onboarding/schemas.ts`:
   - WAŻNE: import z "zod/v4" (NIE "zod")
   - `businessProfileSchema`: industry min(1), role min(1), business_goal min(1).max(200), company_size optional
   - `onboardingChatSchema`: question (string), isComplete (boolean), experience_summary (string) — z .describe() na każdym polu
   - Export typów z z.infer

4. Utwórz `src/lib/onboarding/prompts.ts`:
   - `ONBOARDING_CHAT_SYSTEM_PROMPT` — system prompt dla chatu onboardingu
   - Prompt powinien: przyjąć kontekst branży/roli/celu, zadać 2-3 pytania doprecyzowujące o doświadczeniu biznesowym, generować experience_summary (2-3 zdania) gdy isComplete=true
   - Po polsku, tone: profesjonalny ale przyjazny
   - Zawiera placeholdery {industry}, {role}, {goal} do zamiany

5. Dodaj model `onboarding` w `src/lib/ai/providers.ts`:
   - W MODEL_CONFIG dodaj: `onboarding: openaiProvider('gpt-4o-mini')` z komentarzem
   - NIE zmieniaj istniejących modeli
  </action>
  <verify>
    - `npx tsc --noEmit` przechodzi bez błędów
    - Plik migracji istnieje i zawiera CREATE TABLE, RLS, trigger
    - `grep -r "zod/v4" src/lib/onboarding/schemas.ts` zwraca trafienie
    - `grep "onboarding" src/lib/ai/providers.ts` zwraca trafienie
  </verify>
  <done>
    Migracja SQL gotowa do wykonania, typy TS eksportowane, schematy Zod walidują dane formularza i chatu, prompt AI zdefiniowany, model onboarding skonfigurowany w providerach.
  </done>
</task>

<task type="auto">
  <name>Task 2: DAL server actions + shadcn/ui Popover+Command</name>
  <files>
    src/lib/onboarding/onboarding-dal.ts
    src/components/ui/popover.tsx
    src/components/ui/command.tsx
  </files>
  <action>
1. Dodaj komponenty shadcn/ui:
   - Uruchom: `npx shadcn@latest add popover command` (automatycznie doda pliki do src/components/ui/)
   - Zweryfikuj że pliki powstały

2. Utwórz `src/lib/onboarding/onboarding-dal.ts`:
   - "use server" na początku pliku
   - Import createClient z "@/lib/supabase/server"
   - Import revalidatePath z "next/cache"
   - Import BusinessProfileInput z types/onboarding

   Funkcje (wzorzec z gamification-dal.ts — getUser() + query):

   a) `getBusinessProfile()`: Promise<BusinessProfile | null>
      - createClient, getUser, if !user return null
      - SELECT * FROM user_business_profiles WHERE user_id = user.id (.maybeSingle())

   b) `saveBusinessProfile(input: BusinessProfileInput & { experience_summary?: string })`: Promise<{ success: boolean; error?: string }>
      - createClient, getUser, if !user return error
      - UPSERT z onConflict: "user_id" (bezpieczne — UNIQUE na całej kolumnie, NIE partial index)
      - Ustawia onboarding_completed: true
      - revalidatePath("/dashboard"), revalidatePath("/onboarding"), revalidatePath("/profile")
      - Return { success: true } lub { success: false, error }

   c) `isOnboardingCompleted()`: Promise<boolean>
      - createClient, getUser, if !user return false
      - SELECT onboarding_completed WHERE user_id (.maybeSingle())
      - Return data?.onboarding_completed ?? false
  </action>
  <verify>
    - `npx tsc --noEmit` przechodzi bez błędów
    - `ls src/components/ui/popover.tsx src/components/ui/command.tsx` — oba istnieją
    - `grep "use server" src/lib/onboarding/onboarding-dal.ts` — zwraca trafienie
    - `grep "getUser" src/lib/onboarding/onboarding-dal.ts` — zwraca trafienie (NIE getSession)
    - `grep "revalidatePath" src/lib/onboarding/onboarding-dal.ts` — zwraca trafienie
  </verify>
  <done>
    DAL eksportuje getBusinessProfile, saveBusinessProfile, isOnboardingCompleted. Komponenty Popover i Command dostępne w src/components/ui/. Cały fundament gotowy dla formularza, chatu i bannera.
  </done>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` — zero błędów typów
- Migracja SQL poprawna składniowo (CREATE TABLE, RLS, trigger)
- DAL używa "use server", getUser() (nie getSession), revalidatePath
- Zod importowany z "zod/v4"
- Popover i Command w src/components/ui/
- MODEL_CONFIG zawiera klucz "onboarding"
</verification>

<success_criteria>
- Tabela user_business_profiles zdefiniowana z RLS
- Typy, schematy, DAL, prompt gotowe do użycia przez inne plany
- Komponenty shadcn/ui (Popover, Command) dostępne
- Zero nowych npm dependencies (tylko npx shadcn add)
</success_criteria>

<output>
After completion, create `.planning/phases/08-business-onboarding/08-01-SUMMARY.md`
</output>
</output>
