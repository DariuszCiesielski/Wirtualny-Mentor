---
phase: 08-business-onboarding
plan: 02
type: execute
wave: 2
depends_on: ["08-01"]
files_modified:
  - src/components/onboarding/combobox.tsx
  - src/components/onboarding/business-profile-form.tsx
  - src/app/(dashboard)/onboarding/page.tsx
autonomous: true

must_haves:
  truths:
    - "Użytkownik widzi formularz z 4 polami (branża, rola, cel, wielkość firmy) na stronie /onboarding"
    - "Pola Branża i Rola to Combobox (wybór z listy + możliwość wpisania własnej wartości)"
    - "Po wypełnieniu i zapisaniu profil trafia do bazy danych"
    - "Formularz waliduje wymagane pola i pokazuje błędy"
  artifacts:
    - path: "src/components/onboarding/combobox.tsx"
      provides: "Reusable Combobox component"
      min_lines: 50
    - path: "src/components/onboarding/business-profile-form.tsx"
      provides: "Business profile form with 4 fields"
      min_lines: 80
    - path: "src/app/(dashboard)/onboarding/page.tsx"
      provides: "Onboarding page"
      contains: "BusinessProfileForm"
  key_links:
    - from: "src/components/onboarding/business-profile-form.tsx"
      to: "src/lib/onboarding/onboarding-dal.ts"
      via: "saveBusinessProfile server action"
      pattern: "saveBusinessProfile"
    - from: "src/components/onboarding/business-profile-form.tsx"
      to: "src/lib/onboarding/schemas.ts"
      via: "Zod validation"
      pattern: "businessProfileSchema"
    - from: "src/components/onboarding/combobox.tsx"
      to: "src/components/ui/popover"
      via: "Popover + Command composition"
      pattern: "Popover.*Command"
---

<objective>
Formularz profilu biznesowego z komponentem Combobox i strona /onboarding.

Purpose: ONB-01 — użytkownik może wypełnić formularz profilu biznesowego (branża, rola, cel, wielkość firmy).
Output: Komponent Combobox, formularz BusinessProfileForm, strona /onboarding.
</objective>

<execution_context>
@/Users/dariuszciesielski/.claude/get-shit-done/workflows/execute-plan.md
@/Users/dariuszciesielski/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/08-business-onboarding/08-RESEARCH.md
@.planning/phases/08-business-onboarding/08-01-SUMMARY.md

@src/lib/onboarding/onboarding-dal.ts
@src/lib/onboarding/schemas.ts
@src/types/onboarding.ts
@src/components/ui/popover.tsx
@src/components/ui/command.tsx
@src/app/(dashboard)/profile/page.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Komponent Combobox (reusable)</name>
  <files>src/components/onboarding/combobox.tsx</files>
  <action>
Utwórz `src/components/onboarding/combobox.tsx` — reusable Combobox zbudowany z Popover + Command.

Implementacja (wzorzec z RESEARCH.md, sekcja "Combobox Component"):
- "use client"
- Props: options ({value, label}[]), value, onChange, placeholder, searchPlaceholder, emptyMessage, allowCustom (boolean)
- Stan: open (Popover), search (CommandInput)
- Gdy allowCustom=true i brak wyników — pokaż przycisk "Użyj: {search}" w CommandEmpty
- Ikona Check przy wybranej opcji, ChevronsUpDown jako trigger icon
- PopoverContent width dopasowana do triggera: `className="w-[--radix-popover-trigger-width] p-0"`
- Button trigger: variant="outline", role="combobox", className="w-full justify-between font-normal"
- Polskie teksty: "Wybierz...", "Szukaj...", "Nie znaleziono."
  </action>
  <verify>
    - `npx tsc --noEmit` przechodzi
    - Plik eksportuje komponent Combobox z props: options, value, onChange, allowCustom
  </verify>
  <done>Komponent Combobox renderuje dropdown z wyszukiwaniem i opcją wpisania własnej wartości.</done>
</task>

<task type="auto">
  <name>Task 2: Formularz BusinessProfileForm + strona /onboarding</name>
  <files>
    src/components/onboarding/business-profile-form.tsx
    src/app/(dashboard)/onboarding/page.tsx
  </files>
  <action>
1. Utwórz `src/components/onboarding/business-profile-form.tsx`:
   - "use client"
   - Props: initialData? (BusinessProfile | null), onSuccess? callback
   - Stan formularza: industry, role, business_goal, company_size (kontrolowany)
   - Stan UI: loading, errors (Record<string, string>)
   - Walidacja: businessProfileSchema.safeParse() z schemas.ts
   - Submit: wywołaj saveBusinessProfile(input) z DAL
   - Po sukcesie: toast sonner "Profil zapisany", wywołaj onSuccess callback, router.push("/dashboard") jeśli nie ma onSuccess

   Layout (1 ekran, NIE wizard):
   - Card z CardHeader ("Profil biznesowy", opis) + CardContent
   - 4 pola w jednej kolumnie:
     a) Branża — Combobox z allowCustom=true, opcje: ["IT / Technologia", "Marketing / Reklama", "Finanse / Bankowość", "E-commerce", "Edukacja", "Zdrowie / Medycyna", "Produkcja / Przemysł", "Usługi profesjonalne", "Nieruchomości", "Gastronomia / HoReCa", "Transport / Logistyka", "Inne"]
     b) Rola — Combobox z allowCustom=true, opcje: ["Właściciel firmy", "CEO / Dyrektor", "Manager / Kierownik", "Specjalista", "Freelancer / Konsultant", "Student / Początkujący", "Inne"]
     c) Cel biznesowy — Textarea, max 200 znaków, placeholder "Np. Chcę wdrożyć AI w obsłudze klienta"
     d) Wielkość firmy — Select (NIE combobox), opcje: ["1 osoba (soloprzedsiębiorca)", "2-10", "11-50", "51-200", "201-1000", "1000+"], opcjonalne
   - Przycisk "Zapisz profil" (loading state) + przycisk "Doprecyzuj z AI" (secondary, prowadzi do chatu — plan 03)

   WAŻNE:
   - Przycisk "Doprecyzuj z AI" powinien być renderowany warunkowo — widoczny tylko jeśli formularz jest wypełniony (industry + role + business_goal mają wartość). Klik ustawia stan showChat=true (lub nawiguje do #chat anchor). Chat AI będzie dodany w planie 03 — na razie przycisk może być disabled z tooltipem "Wkrótce dostępne" LUB po prostu nie renderowany. Lepiej: renderuj przycisk ale z onClick który ustawia np. lokalną flagę. Plan 03 doda komponent OnboardingChat który reaguje na tę flagę.
   - Formularz musi być KONTROLOWANY (useState na każdym polu) — NIE uncontrolled
   - Jeśli initialData (edycja) — pre-fill pola

2. Utwórz `src/app/(dashboard)/onboarding/page.tsx`:
   - Server component
   - Wywołaj getBusinessProfile() z DAL
   - Renderuj BusinessProfileForm z initialData={profile}
   - Tytuł strony: metadata export { title: "Profil biznesowy" }
   - Layout: max-w-2xl mx-auto py-8 px-4
  </action>
  <verify>
    - `npx tsc --noEmit` przechodzi
    - `npm run build` przechodzi (strona /onboarding buduje się poprawnie)
    - Formularz renderuje 4 pola: 2x Combobox, 1x Textarea, 1x Select
    - saveBusinessProfile jest wywoływany przy submit
  </verify>
  <done>
    Strona /onboarding wyświetla formularz 4 pól z walidacją Zod. Użytkownik może wybrać branżę/rolę z listy lub wpisać własną wartość. Po zapisaniu profil trafia do DB przez DAL.
  </done>
</task>

</tasks>

<verification>
- `npm run build` — zero błędów
- /onboarding renderuje formularz z 4 polami
- Combobox pozwala na wybór z listy i wpisanie własnej wartości
- Submit zapisuje profil przez saveBusinessProfile
- Walidacja Zod blokuje submit z pustymi wymaganymi polami
</verification>

<success_criteria>
- ONB-01 spełniany: formularz profilu biznesowego z 4 polami (branża, rola, cel, wielkość firmy)
- Combobox działa z listą opcji + free-text
- Strona /onboarding jest chroniona auth (layout dashboard)
- Formularz akceptuje initialData do edycji (reuse w /profile)
</success_criteria>

<output>
After completion, create `.planning/phases/08-business-onboarding/08-02-SUMMARY.md`
</output>
</output>
