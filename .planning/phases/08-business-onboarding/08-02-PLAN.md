---
phase: 08-business-onboarding
plan: 02
type: execute
wave: 2
depends_on: ["08-01"]
files_modified:
  - src/components/onboarding/combobox.tsx
  - src/components/onboarding/business-profile-form.tsx
  - src/app/(dashboard)/onboarding/page.tsx
autonomous: true

must_haves:
  truths:
    - "Uzytkownik widzi formularz z 4 polami (branza, rola, cel, wielkosc firmy) na stronie /onboarding"
    - "Pola Branza i Rola to Combobox (wybor z listy + mozliwosc wpisania wlasnej wartosci)"
    - "Po wypelnieniu i zapisaniu profil trafia do bazy danych"
    - "Formularz waliduje wymagane pola i pokazuje bledy"
  artifacts:
    - path: "src/components/onboarding/combobox.tsx"
      provides: "Reusable Combobox component"
      min_lines: 50
    - path: "src/components/onboarding/business-profile-form.tsx"
      provides: "Business profile form with 4 fields"
      min_lines: 80
    - path: "src/app/(dashboard)/onboarding/page.tsx"
      provides: "Onboarding page"
      contains: "BusinessProfileForm"
  key_links:
    - from: "src/components/onboarding/business-profile-form.tsx"
      to: "src/lib/onboarding/onboarding-dal.ts"
      via: "saveBusinessProfile server action"
      pattern: "saveBusinessProfile"
    - from: "src/components/onboarding/business-profile-form.tsx"
      to: "src/lib/onboarding/schemas.ts"
      via: "Zod validation"
      pattern: "businessProfileSchema"
    - from: "src/components/onboarding/combobox.tsx"
      to: "src/components/ui/popover"
      via: "Popover + Command composition"
      pattern: "Popover.*Command"
---

<objective>
Formularz profilu biznesowego z komponentem Combobox i strona /onboarding.

Purpose: ONB-01 — uzytkownik moze wypelnic formularz profilu biznesowego (branza, rola, cel, wielkosc firmy).
Output: Komponent Combobox, formularz BusinessProfileForm, strona /onboarding.
</objective>

<execution_context>
@/Users/dariuszciesielski/.claude/get-shit-done/workflows/execute-plan.md
@/Users/dariuszciesielski/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/08-business-onboarding/08-RESEARCH.md
@.planning/phases/08-business-onboarding/08-01-SUMMARY.md

@src/lib/onboarding/onboarding-dal.ts
@src/lib/onboarding/schemas.ts
@src/types/onboarding.ts
@src/components/ui/popover.tsx
@src/components/ui/command.tsx
@src/app/(dashboard)/profile/page.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Komponent Combobox (reusable)</name>
  <files>src/components/onboarding/combobox.tsx</files>
  <action>
Utworz `src/components/onboarding/combobox.tsx` — reusable Combobox zbudowany z Popover + Command.

Implementacja (wzorzec z RESEARCH.md, sekcja "Combobox Component"):
- "use client"
- Props: options ({value, label}[]), value, onChange, placeholder, searchPlaceholder, emptyMessage, allowCustom (boolean)
- Stan: open (Popover), search (CommandInput)
- Gdy allowCustom=true i brak wynikow — pokaz przycisk "Uzyj: {search}" w CommandEmpty
- Ikona Check przy wybranej opcji, ChevronsUpDown jako trigger icon
- PopoverContent width dopasowana do triggera: `className="w-[--radix-popover-trigger-width] p-0"`
- Button trigger: variant="outline", role="combobox", className="w-full justify-between font-normal"
- Polskie teksty: "Wybierz...", "Szukaj...", "Nie znaleziono."
  </action>
  <verify>
    - `npx tsc --noEmit` przechodzi
    - Plik eksportuje komponent Combobox z props: options, value, onChange, allowCustom
  </verify>
  <done>Komponent Combobox renderuje dropdown z wyszukiwaniem i opcja wpisania wlasnej wartosci.</done>
</task>

<task type="auto">
  <name>Task 2: Formularz BusinessProfileForm + strona /onboarding</name>
  <files>
    src/components/onboarding/business-profile-form.tsx
    src/app/(dashboard)/onboarding/page.tsx
  </files>
  <action>
1. Utworz `src/components/onboarding/business-profile-form.tsx`:
   - "use client"
   - Props: initialData? (BusinessProfile | null), onSuccess? callback
   - Stan formularza: industry, role, business_goal, company_size (kontrolowany)
   - Stan UI: loading, errors (Record<string, string>)
   - Walidacja: businessProfileSchema.safeParse() z schemas.ts
   - Submit: wywolaj saveBusinessProfile(input) z DAL
   - Po sukcesie: toast sonner "Profil zapisany", wywolaj onSuccess callback, router.push("/dashboard") jesli nie ma onSuccess

   Layout (1 ekran, NIE wizard):
   - Card z CardHeader ("Profil biznesowy", opis) + CardContent
   - 4 pola w jednej kolumnie:
     a) Branza — Combobox z allowCustom=true, opcje: ["IT / Technologia", "Marketing / Reklama", "Finanse / Bankowosc", "E-commerce", "Edukacja", "Zdrowie / Medycyna", "Produkcja / Przemysl", "Uslugi profesjonalne", "Nieruchomosci", "Gastronomia / HoReCa", "Transport / Logistyka", "Inne"]
     b) Rola — Combobox z allowCustom=true, opcje: ["Wlasciciel firmy", "CEO / Dyrektor", "Manager / Kierownik", "Specjalista", "Freelancer / Konsultant", "Student / Poczatkujacy", "Inne"]
     c) Cel biznesowy — Textarea, max 200 znakow, placeholder "Np. Chce wdrozyc AI w obsludze klienta"
     d) Wielkosc firmy — Select (NIE combobox), opcje: ["1 osoba (soloprzedsiebiorca)", "2-10", "11-50", "51-200", "201-1000", "1000+"], opcjonalne
   - Przycisk "Zapisz profil" (loading state) + przycisk "Doprecyzuj z AI" (secondary, prowadzi do chatu — plan 03)

   WAZNE:
   - Przycisk "Doprecyzuj z AI" powinien byc renderowany warunkowo — widoczny tylko jesli formularz jest wypelniony (industry + role + business_goal maja wartosc). Klik ustawia stan showChat=true (lub naviguje do #chat anchor). Chat AI bedzie dodany w planie 03 — na razie przycisk moze byc disabled z tooltipem "Wkrotce dostepne" LUB po prostu nie renderowany. Lepiej: renderuj przycisk ale z onClick ktory ustawia np. lokalna flage. Plan 03 doda komponent OnboardingChat ktory reaguje na te flage.
   - Formularz musi byc KONTROLOWANY (useState na kazdym polu) — NIE uncontrolled
   - Jesli initialData (edycja) — pre-fill pola

2. Utworz `src/app/(dashboard)/onboarding/page.tsx`:
   - Server component
   - Wywolaj getBusinessProfile() z DAL
   - Renderuj BusinessProfileForm z initialData={profile}
   - Tytul strony: metadata export { title: "Profil biznesowy" }
   - Layout: max-w-2xl mx-auto py-8 px-4
  </action>
  <verify>
    - `npx tsc --noEmit` przechodzi
    - `npm run build` przechodzi (strona /onboarding buduje sie poprawnie)
    - Formularz renderuje 4 pola: 2x Combobox, 1x Textarea, 1x Select
    - saveBusinessProfile jest wywolywany przy submit
  </verify>
  <done>
    Strona /onboarding wyswietla formularz 4 pol z walidacja Zod. Uzytkownik moze wybrac branze/role z listy lub wpisac wlasna wartosc. Po zapisaniu profil trafia do DB przez DAL.
  </done>
</task>

</tasks>

<verification>
- `npm run build` — zero bledow
- /onboarding renderuje formularz z 4 polami
- Combobox pozwala na wybor z listy i wpisanie wlasnej wartosci
- Submit zapisuje profil przez saveBusinessProfile
- Walidacja Zod blokuje submit z pustymi wymaganymi polami
</verification>

<success_criteria>
- ONB-01 spelniany: formularz profilu biznesowego z 4 polami (branza, rola, cel, wielkosc firmy)
- Combobox dziala z lista opcji + free-text
- Strona /onboarding jest chroniona auth (layout dashboard)
- Formularz akceptuje initialData do edycji (reuse w /profile)
</success_criteria>

<output>
After completion, create `.planning/phases/08-business-onboarding/08-02-SUMMARY.md`
</output>
