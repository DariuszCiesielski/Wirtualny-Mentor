---
phase: 08-business-onboarding
plan: 03
type: execute
wave: 2
depends_on: ["08-01"]
files_modified:
  - src/app/api/onboarding/chat/route.ts
  - src/components/onboarding/onboarding-chat.tsx
  - src/components/onboarding/onboarding-banner.tsx
  - src/app/(dashboard)/dashboard/page.tsx
autonomous: true

must_haves:
  truths:
    - "Użytkownik może prowadzić krótki chat z AI który zadaje 2-3 pytania o kontekście biznesowym"
    - "AI generuje experience_summary po zebraniu wystarczających informacji"
    - "Dashboard wyświetla banner zachęcający do uzupełnienia profilu gdy onboarding nie ukończony"
    - "Banner znika na stałe po ukończeniu onboardingu i tymczasowo po kliknięciu X"
  artifacts:
    - path: "src/app/api/onboarding/chat/route.ts"
      provides: "Streaming chat API endpoint"
      contains: "streamText"
    - path: "src/components/onboarding/onboarding-chat.tsx"
      provides: "Chat UI component"
      contains: "useChat"
    - path: "src/components/onboarding/onboarding-banner.tsx"
      provides: "Dashboard banner component"
      contains: "OnboardingBanner"
    - path: "src/app/(dashboard)/dashboard/page.tsx"
      provides: "Dashboard with banner integration"
      contains: "OnboardingBanner"
  key_links:
    - from: "src/components/onboarding/onboarding-chat.tsx"
      to: "src/app/api/onboarding/chat/route.ts"
      via: "useChat with DefaultChatTransport"
      pattern: "DefaultChatTransport.*onboarding/chat"
    - from: "src/app/api/onboarding/chat/route.ts"
      to: "src/lib/onboarding/prompts.ts"
      via: "system prompt import"
      pattern: "ONBOARDING_CHAT_SYSTEM_PROMPT"
    - from: "src/app/(dashboard)/dashboard/page.tsx"
      to: "src/lib/onboarding/onboarding-dal.ts"
      via: "isOnboardingCompleted check"
      pattern: "isOnboardingCompleted"
---

<objective>
Chat AI onboardingu i banner na dashboardzie.

Purpose: ONB-02 (chat AI doprecyzowujący), ONB-03 (AI generuje experience_summary), ONB-04 (banner na dashboardzie).
Output: API endpoint chatu, komponent chatu, banner na dashboardzie.
</objective>

<execution_context>
@/Users/dariuszciesielski/.claude/get-shit-done/workflows/execute-plan.md
@/Users/dariuszciesielski/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/08-business-onboarding/08-RESEARCH.md
@.planning/phases/08-business-onboarding/08-01-SUMMARY.md

@src/lib/onboarding/onboarding-dal.ts
@src/lib/onboarding/schemas.ts
@src/lib/onboarding/prompts.ts
@src/lib/ai/providers.ts
@src/components/curriculum/clarifying-chat.tsx
@src/app/(dashboard)/dashboard/page.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: API endpoint chatu + komponent OnboardingChat</name>
  <files>
    src/app/api/onboarding/chat/route.ts
    src/components/onboarding/onboarding-chat.tsx
  </files>
  <action>
1. Utwórz `src/app/api/onboarding/chat/route.ts`:
   - Wzorzec z ClarifyingChat (RESEARCH.md Pattern 2)
   - Import: streamText, Output z "ai", getModel z providers, onboardingChatSchema z schemas, ONBOARDING_CHAT_SYSTEM_PROMPT z prompts
   - Auth: createClient() + getUser() — odrzuć jeśli !user (401)
   - Parsuj body: { messages, profileData } (profileData to {industry, role, business_goal})
   - Zbuduj systemPrompt przez zamianę placeholderów {industry}, {role}, {goal} w ONBOARDING_CHAT_SYSTEM_PROMPT
   - Wywołaj streamText z: model: getModel("onboarding"), system: systemPrompt, messages, experimental_output: Output.object({ schema: onboardingChatSchema })
   - Return result.toUIMessageStreamResponse()
   - maxDuration: 30 (export const maxDuration = 30)

2. Utwórz `src/components/onboarding/onboarding-chat.tsx`:
   - "use client"
   - Wzorzec z ClarifyingChat (useChat + DefaultChatTransport + parsowanie parts)
   - Props: profileData ({industry, role, business_goal}), onComplete (callback z experience_summary: string)
   - useChat z: transport: new DefaultChatTransport({ url: "/api/onboarding/chat" }), body: { profileData }
   - Parsowanie response: z message.parts szukaj tool-invocation lub text part z JSON, lub użyj experimental_output parsing (sprawdź wzorzec w ClarifyingChat)
   - Max turns: po 5 wiadomościach usera LUB gdy AI zwróci isComplete=true, pokaż przycisk "Zakończ i zapisz"
   - Przycisk "Zakończ i zapisz": wywołaj saveBusinessProfile z experience_summary, potem onComplete
   - Przycisk "Przejdź dalej bez chatu": pozwala pominąć chat (wywołaj onComplete bez experience_summary)
   - UI: lista wiadomości (proste div-y, nie trzeba pełnego chat UI), input z przyciskiem wyślij
   - Card wrapper z tytuł "Doprecyzuj z AI" i opis
   - Loading state na przyciskach
   - WAŻNE: Sprawdź dokładnie jak ClarifyingChat parsuje structured output z message parts — skopiuj ten sam pattern. W AI SDK v6 structured output pojawia się w message.parts jako { type: "text", text: JSON } lub { type: "tool-invocation" }. Wzorzec z ClarifyingChat (linia ~200-260) jest kluczowy.
  </action>
  <verify>
    - `npx tsc --noEmit` przechodzi
    - API route eksportuje POST handler
    - Komponent używa useChat z DefaultChatTransport
    - Max turns limit jest zaimplementowany
  </verify>
  <done>
    Chat AI onboardingu: użytkownik może prowadzić krótką rozmowę (2-5 wiadomości), AI generuje experience_summary z odpowiedzi.
  </done>
</task>

<task type="auto">
  <name>Task 2: Banner na dashboardzie + integracja</name>
  <files>
    src/components/onboarding/onboarding-banner.tsx
    src/app/(dashboard)/dashboard/page.tsx
  </files>
  <action>
1. Utwórz `src/components/onboarding/onboarding-banner.tsx`:
   - "use client"
   - Wzorzec z RESEARCH.md (Banner Component)
   - useState(false) dla dismissed — klik X ukrywa banner do końca sesji
   - Ikony: Briefcase (główna), X (zamknij), ArrowRight (link)
   - Link do /onboarding z tekstem "Uzupełnij profil"
   - Tekst: "Uzupełnij profil biznesowy" + "Pomóż AI lepiej dopasować kursy do Twojej branży i celów."
   - Styling: rounded-lg border bg-primary/5 p-4 mb-6
   - NIE persystuj dismiss w DB — tylko useState (decyzja z CONTEXT.md)
   - Props: brak (komponent jest self-contained)

2. Zmodyfikuj `src/app/(dashboard)/dashboard/page.tsx`:
   - Import isOnboardingCompleted z onboarding-dal.ts
   - Import OnboardingBanner (dynamic import z ssr: false — client component)
   - Na początku komponentu (po auth, obok getUserCourses): wywołaj isOnboardingCompleted()
   - Jeśli !onboardingCompleted: renderuj <OnboardingBanner /> na początku contentu (przed sekcją powitalną/kursami)
   - WAŻNE: isOnboardingCompleted jest server action — wywołaj ją bezpośrednio w server component (await)
   - NIE używaj dynamic() jeśli można uniknąć — OnboardingBanner jest client component ale może być renderowany warunkowo w server component. Użyj warunkowego renderowania: {!onboardingCompleted && <OnboardingBanner />}
  </action>
  <verify>
    - `npx tsc --noEmit` przechodzi
    - `npm run build` przechodzi
    - Dashboard page importuje i renderuje OnboardingBanner warunkowo
    - Banner ma przycisk X który go ukrywa (useState)
    - Banner linkuje do /onboarding
  </verify>
  <done>
    Banner na dashboardzie zachęca do uzupełnienia profilu. Znika na stałe po ukończeniu onboardingu (isOnboardingCompleted=true). Klik X ukrywa go tymczasowo do końca sesji.
  </done>
</task>

</tasks>

<verification>
- `npm run build` — zero błędów
- API /api/onboarding/chat odpowiada na POST z streamingiem
- Chat ma limit turns (max 5) i przycisk zakończenia
- Banner wyświetla się na dashboardzie gdy onboarding nie ukończony
- Banner znika po kliknięciu X (sesja) i po ukończeniu onboardingu (permanentnie)
</verification>

<success_criteria>
- ONB-02: Chat AI zadaje 2-3 pytania doprecyzowujące
- ONB-03: AI generuje experience_summary po zakończeniu chatu
- ONB-04: Dashboard banner zachęca do uzupełnienia profilu, znika po onboardingu
</success_criteria>

<output>
After completion, create `.planning/phases/08-business-onboarding/08-03-SUMMARY.md`
</output>
</output>
