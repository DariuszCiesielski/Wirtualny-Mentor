---
phase: 08-business-onboarding
plan: 04
type: execute
wave: 3
depends_on: ["08-02", "08-03"]
files_modified:
  - src/app/(dashboard)/onboarding/page.tsx
  - src/components/onboarding/business-profile-form.tsx
  - src/app/(dashboard)/profile/page.tsx
  - src/app/api/curriculum/clarify/route.ts
  - src/app/api/curriculum/generate/route.ts
autonomous: true

must_haves:
  truths:
    - "Strona /onboarding laczy formularz z chatem AI w jednym flow"
    - "Uzytkownik moze edytowac profil biznesowy ze strony /profile"
    - "Przy tworzeniu kursu AI uwzglednia profil biznesowy w pytaniach doprecyzowujacych"
    - "Bez profilu biznesowego generowanie kursow dziala bez zmian"
  artifacts:
    - path: "src/app/(dashboard)/onboarding/page.tsx"
      provides: "Full onboarding flow (form + chat)"
      contains: "OnboardingChat"
    - path: "src/app/(dashboard)/profile/page.tsx"
      provides: "Profile page with business section"
      contains: "BusinessProfileForm"
    - path: "src/app/api/curriculum/clarify/route.ts"
      provides: "Clarify endpoint with business context injection"
      contains: "businessProfile"
    - path: "src/app/api/curriculum/generate/route.ts"
      provides: "Generate endpoint with business context injection"
      contains: "businessProfile"
  key_links:
    - from: "src/app/api/curriculum/clarify/route.ts"
      to: "src/lib/onboarding/onboarding-dal.ts"
      via: "getBusinessProfile server-side"
      pattern: "getBusinessProfile"
    - from: "src/app/api/curriculum/generate/route.ts"
      to: "src/lib/onboarding/onboarding-dal.ts"
      via: "getBusinessProfile server-side"
      pattern: "getBusinessProfile"
    - from: "src/app/(dashboard)/profile/page.tsx"
      to: "src/components/onboarding/business-profile-form.tsx"
      via: "BusinessProfileForm reuse"
      pattern: "BusinessProfileForm"
---

<objective>
Integracja: polaczenie formularza z chatem na /onboarding, sekcja biznesowa na /profile, wstrzykniecie profilu do generowania kursow.

Purpose: ONB-05 (edycja z /profile), ONB-06 (profil wplywa na generowanie kursow), oraz sklejenie formularza z chatem w pelny flow onboardingu.
Output: Kompletny flow onboardingu, sekcja biznesowa w profilu, personalizacja generowania kursow.
</objective>

<execution_context>
@/Users/dariuszciesielski/.claude/get-shit-done/workflows/execute-plan.md
@/Users/dariuszciesielski/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/08-business-onboarding/08-RESEARCH.md
@.planning/phases/08-business-onboarding/08-02-SUMMARY.md
@.planning/phases/08-business-onboarding/08-03-SUMMARY.md

@src/app/(dashboard)/onboarding/page.tsx
@src/components/onboarding/business-profile-form.tsx
@src/components/onboarding/onboarding-chat.tsx
@src/app/(dashboard)/profile/page.tsx
@src/app/api/curriculum/clarify/route.ts
@src/app/api/curriculum/generate/route.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Pelny flow /onboarding (formularz + chat) + sekcja biznesowa w /profile</name>
  <files>
    src/app/(dashboard)/onboarding/page.tsx
    src/components/onboarding/business-profile-form.tsx
    src/app/(dashboard)/profile/page.tsx
  </files>
  <action>
1. Zaktualizuj `src/app/(dashboard)/onboarding/page.tsx`:
   - Server component laduje profil: getBusinessProfile()
   - Renderuje wrapper client component (np. OnboardingFlow) ktory zarzadza stanem: step="form" | "chat" | "done"
   - Krok "form": BusinessProfileForm z onSuccess ktory przechodzi do "chat" (jesli uzytkownik kliknal "Doprecyzuj z AI") lub "done" (jesli kliknal "Zapisz profil")
   - Krok "chat": OnboardingChat z profileData i onComplete ktory przechodzi do "done"
   - Krok "done": komunikat sukcesu + link do dashboardu
   - LUB: prostszy pattern — state management w jednym client component na stronie

2. Zaktualizuj `src/components/onboarding/business-profile-form.tsx`:
   - Dodaj callback props: onSaveAndChat (opcjonalny) — klikniety przez "Doprecyzuj z AI"
   - Przycisk "Doprecyzuj z AI": waliduje formularz, zapisuje profil (saveBusinessProfile), potem wywoluje onSaveAndChat z danymi formularza
   - Przycisk "Zapisz profil": waliduje, zapisuje, wywoluje onSuccess
   - Oba przyciski najpierw zapisuja profil do DB, roznica to co dzieje sie POTEM

3. Zaktualizuj `src/app/(dashboard)/profile/page.tsx`:
   - Dodaj sekcje "Profil biznesowy" pod istniejaca sekcja profilu
   - Import getBusinessProfile z DAL
   - Renderuj BusinessProfileForm z initialData={businessProfile}
   - Sekcja w Card z tytul "Profil biznesowy" i opis
   - NIE dodawaj chatu na stronie /profile — tu jest tylko formularz edycji
   - Separator (Separator z shadcn lub <hr>) miedzy sekcja profilu a sekcja biznesowa
  </action>
  <verify>
    - `npx tsc --noEmit` przechodzi
    - `npm run build` przechodzi
    - /onboarding: formularz -> opcjonalny chat -> sukces (pelny flow)
    - /profile: wyswietla sekcje "Profil biznesowy" z BusinessProfileForm
  </verify>
  <done>
    Pelny flow onboardingu: formularz (4 pola) -> opcjonalny chat AI -> zapis do DB. Strona /profile zawiera sekcje edycji profilu biznesowego.
  </done>
</task>

<task type="auto">
  <name>Task 2: Wstrzykniecie profilu biznesowego do curriculum AI</name>
  <files>
    src/app/api/curriculum/clarify/route.ts
    src/app/api/curriculum/generate/route.ts
  </files>
  <action>
1. Zmodyfikuj `src/app/api/curriculum/clarify/route.ts`:
   - Import getBusinessProfile z onboarding-dal.ts
   - Po auth check (getUser), wywolaj getBusinessProfile() (server-side, zero zmian w frontendzie)
   - Jesli businessProfile istnieje, dopisz do systemPrompt (RESEARCH.md Pattern 3):
     ```
     KONTEKST BIZNESOWY UZYTKOWNIKA:
     - Branza: {industry}
     - Rola: {role}
     - Cel biznesowy: {business_goal}
     - Podsumowanie: {experience_summary} (jesli istnieje)

     DODATKOWE ZASADY:
     - Zadaj 1-2 pytania doprecyzowujace w kontekscie biznesowym uzytkownika
     - Proponuj praktyczne przyklady z branzy uzytkownika
     ```
   - Jesli !businessProfile — NIE zmieniaj promptu (standardowy flow bez zmian)
   - WAZNE: Laduj profil server-side (getBusinessProfile uzywa getUser wewnetrznie). Alternatywnie — jesli endpoint NIE uzywa server actions bezposrednio (route handler), to musisz przekazac supabase client lub uzyc createClient() w route handler i recznie query. Sprawdz czy getBusinessProfile() dziala w kontekscie route handlera (powinno — "use server" jest kompatybilne z route handlers w Next.js 16).

2. Zmodyfikuj `src/app/api/curriculum/generate/route.ts`:
   - Import getBusinessProfile z onboarding-dal.ts
   - Po auth check, wywolaj getBusinessProfile()
   - Jesli businessProfile, dodaj do user prompt (NIE system prompt):
     ```
     ## Kontekst biznesowy uzytkownika:
     - Branza: {industry}
     - Rola: {role}
     - Cel: {business_goal}
     - Doswiadczenie: {experience_summary} (jesli istnieje)

     Dostosuj przyklady i case studies do branzy uzytkownika.
     ```
   - Jesli !businessProfile — NIE zmieniaj promptu
   - WAZNE: NIE zmieniaj struktury response, schematu Zod ani frontendu. Tylko prompt jest bogatszy.
  </action>
  <verify>
    - `npx tsc --noEmit` przechodzi
    - `npm run build` przechodzi
    - `grep "getBusinessProfile" src/app/api/curriculum/clarify/route.ts` — trafienie
    - `grep "getBusinessProfile" src/app/api/curriculum/generate/route.ts` — trafienie
    - Istniejace testy (jesli sa) nadal przechodza
    - Bez profilu biznesowego — API dziala identycznie jak wczesniej (brak regresji)
  </verify>
  <done>
    API clarify i generate uwzgledniaja profil biznesowy uzytkownika w promptach AI. Bez profilu — standardowy flow bez zmian. Zero zmian w interfejsie frontendu.
  </done>
</task>

</tasks>

<verification>
- `npm run build` — zero bledow
- /onboarding: pelny flow formularz -> chat -> sukces
- /profile: sekcja "Profil biznesowy" z formularzem edycji
- /api/curriculum/clarify: dodaje kontekst biznesowy do system promptu (jesli profil istnieje)
- /api/curriculum/generate: dodaje kontekst biznesowy do user promptu (jesli profil istnieje)
- Bez profilu: oba endpointy dzialaja identycznie jak wczesniej
</verification>

<success_criteria>
- ONB-05: Edycja profilu biznesowego ze strony /profile dziala
- ONB-06: Generowanie kursow uwzglednia profil (widoczne w pytaniach AI i strukturze kursu)
- Zero regresji: istniejace kursy bez profilu generuja sie normalnie
- Pelny flow onboardingu: formularz -> opcjonalny chat -> dashboard
</success_criteria>

<output>
After completion, create `.planning/phases/08-business-onboarding/08-04-SUMMARY.md`
</output>
