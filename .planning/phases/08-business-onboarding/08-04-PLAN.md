---
phase: 08-business-onboarding
plan: 04
type: execute
wave: 3
depends_on: ["08-02", "08-03"]
files_modified:
  - src/app/(dashboard)/onboarding/page.tsx
  - src/components/onboarding/business-profile-form.tsx
  - src/app/(dashboard)/profile/page.tsx
  - src/app/api/curriculum/clarify/route.ts
  - src/app/api/curriculum/generate/route.ts
autonomous: true

must_haves:
  truths:
    - "Strona /onboarding łączy formularz z chatem AI w jednym flow"
    - "Użytkownik może edytować profil biznesowy ze strony /profile"
    - "Przy tworzeniu kursu AI uwzględnia profil biznesowy w pytaniach doprecyzowujących"
    - "Bez profilu biznesowego generowanie kursów działa bez zmian"
  artifacts:
    - path: "src/app/(dashboard)/onboarding/page.tsx"
      provides: "Full onboarding flow (form + chat)"
      contains: "OnboardingChat"
    - path: "src/app/(dashboard)/profile/page.tsx"
      provides: "Profile page with business section"
      contains: "BusinessProfileForm"
    - path: "src/app/api/curriculum/clarify/route.ts"
      provides: "Clarify endpoint with business context injection"
      contains: "businessProfile"
    - path: "src/app/api/curriculum/generate/route.ts"
      provides: "Generate endpoint with business context injection"
      contains: "businessProfile"
  key_links:
    - from: "src/app/api/curriculum/clarify/route.ts"
      to: "src/lib/onboarding/onboarding-dal.ts"
      via: "getBusinessProfile server-side"
      pattern: "getBusinessProfile"
    - from: "src/app/api/curriculum/generate/route.ts"
      to: "src/lib/onboarding/onboarding-dal.ts"
      via: "getBusinessProfile server-side"
      pattern: "getBusinessProfile"
    - from: "src/app/(dashboard)/profile/page.tsx"
      to: "src/components/onboarding/business-profile-form.tsx"
      via: "BusinessProfileForm reuse"
      pattern: "BusinessProfileForm"
---

<objective>
Integracja: połączenie formularza z chatem na /onboarding, sekcja biznesowa na /profile, wstrzyknięcie profilu do generowania kursów.

Purpose: ONB-05 (edycja z /profile), ONB-06 (profil wpływa na generowanie kursów), oraz sklejenie formularza z chatem w pełny flow onboardingu.
Output: Kompletny flow onboardingu, sekcja biznesowa w profilu, personalizacja generowania kursów.
</objective>

<execution_context>
@/Users/dariuszciesielski/.claude/get-shit-done/workflows/execute-plan.md
@/Users/dariuszciesielski/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/08-business-onboarding/08-RESEARCH.md
@.planning/phases/08-business-onboarding/08-02-SUMMARY.md
@.planning/phases/08-business-onboarding/08-03-SUMMARY.md

@src/app/(dashboard)/onboarding/page.tsx
@src/components/onboarding/business-profile-form.tsx
@src/components/onboarding/onboarding-chat.tsx
@src/app/(dashboard)/profile/page.tsx
@src/app/api/curriculum/clarify/route.ts
@src/app/api/curriculum/generate/route.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Pełny flow /onboarding (formularz + chat) + sekcja biznesowa w /profile</name>
  <files>
    src/app/(dashboard)/onboarding/page.tsx
    src/components/onboarding/business-profile-form.tsx
    src/app/(dashboard)/profile/page.tsx
  </files>
  <action>
1. Zaktualizuj `src/app/(dashboard)/onboarding/page.tsx`:
   - Server component ładuje profil: getBusinessProfile()
   - Renderuje wrapper client component (np. OnboardingFlow) który zarządza stanem: step="form" | "chat" | "done"
   - Krok "form": BusinessProfileForm z onSuccess który przechodzi do "chat" (jeśli użytkownik kliknął "Doprecyzuj z AI") lub "done" (jeśli kliknął "Zapisz profil")
   - Krok "chat": OnboardingChat z profileData i onComplete który przechodzi do "done"
   - Krok "done": komunikat sukcesu + link do dashboardu
   - LUB: prostszy pattern — state management w jednym client component na stronie

2. Zaktualizuj `src/components/onboarding/business-profile-form.tsx`:
   - Dodaj callback props: onSaveAndChat (opcjonalny) — kliknięty przez "Doprecyzuj z AI"
   - Przycisk "Doprecyzuj z AI": waliduje formularz, zapisuje profil (saveBusinessProfile), potem wywołuje onSaveAndChat z danymi formularza
   - Przycisk "Zapisz profil": waliduje, zapisuje, wywołuje onSuccess
   - Oba przyciski najpierw zapisują profil do DB, różnica to co dzieje się POTEM

3. Zaktualizuj `src/app/(dashboard)/profile/page.tsx`:
   - Dodaj sekcję "Profil biznesowy" pod istniejącą sekcją profilu
   - Import getBusinessProfile z DAL
   - Renderuj BusinessProfileForm z initialData={businessProfile}
   - Sekcja w Card z tytuł "Profil biznesowy" i opis
   - NIE dodawaj chatu na stronie /profile — tu jest tylko formularz edycji
   - Separator (Separator z shadcn lub <hr>) między sekcją profilu a sekcją biznesową
  </action>
  <verify>
    - `npx tsc --noEmit` przechodzi
    - `npm run build` przechodzi
    - /onboarding: formularz -> opcjonalny chat -> sukces (pełny flow)
    - /profile: wyświetla sekcję "Profil biznesowy" z BusinessProfileForm
  </verify>
  <done>
    Pełny flow onboardingu: formularz (4 pola) -> opcjonalny chat AI -> zapis do DB. Strona /profile zawiera sekcję edycji profilu biznesowego.
  </done>
</task>

<task type="auto">
  <name>Task 2: Wstrzyknięcie profilu biznesowego do curriculum AI</name>
  <files>
    src/app/api/curriculum/clarify/route.ts
    src/app/api/curriculum/generate/route.ts
  </files>
  <action>
1. Zmodyfikuj `src/app/api/curriculum/clarify/route.ts`:
   - Import getBusinessProfile z onboarding-dal.ts
   - Po auth check (getUser), wywołaj getBusinessProfile() (server-side, zero zmian w frontendzie)
   - Jeśli businessProfile istnieje, dopisz do systemPrompt (RESEARCH.md Pattern 3):
     ```
     KONTEKST BIZNESOWY UŻYTKOWNIKA:
     - Branża: {industry}
     - Rola: {role}
     - Cel biznesowy: {business_goal}
     - Podsumowanie: {experience_summary} (jeśli istnieje)

     DODATKOWE ZASADY:
     - Zadaj 1-2 pytania doprecyzowujące w kontekście biznesowym użytkownika
     - Proponuj praktyczne przykłady z branży użytkownika
     ```
   - Jeśli !businessProfile — NIE zmieniaj promptu (standardowy flow bez zmian)
   - WAŻNE: Ładuj profil server-side (getBusinessProfile używa getUser wewnętrznie). Alternatywnie — jeśli endpoint NIE używa server actions bezpośrednio (route handler), to musisz przekazać supabase client lub użyć createClient() w route handler i ręcznie query. Sprawdź czy getBusinessProfile() działa w kontekście route handlera (powinno — "use server" jest kompatybilne z route handlers w Next.js 16).

2. Zmodyfikuj `src/app/api/curriculum/generate/route.ts`:
   - Import getBusinessProfile z onboarding-dal.ts
   - Po auth check, wywołaj getBusinessProfile()
   - Jeśli businessProfile, dodaj do user prompt (NIE system prompt):
     ```
     ## Kontekst biznesowy użytkownika:
     - Branża: {industry}
     - Rola: {role}
     - Cel: {business_goal}
     - Doświadczenie: {experience_summary} (jeśli istnieje)

     Dostosuj przykłady i case studies do branży użytkownika.
     ```
   - Jeśli !businessProfile — NIE zmieniaj promptu
   - WAŻNE: NIE zmieniaj struktury response, schematu Zod ani frontendu. Tylko prompt jest bogatszy.
  </action>
  <verify>
    - `npx tsc --noEmit` przechodzi
    - `npm run build` przechodzi
    - `grep "getBusinessProfile" src/app/api/curriculum/clarify/route.ts` — trafienie
    - `grep "getBusinessProfile" src/app/api/curriculum/generate/route.ts` — trafienie
    - Istniejące testy (jeśli są) nadal przechodzą
    - Bez profilu biznesowego — API działa identycznie jak wcześniej (brak regresji)
  </verify>
  <done>
    API clarify i generate uwzględniają profil biznesowy użytkownika w promptach AI. Bez profilu — standardowy flow bez zmian. Zero zmian w interfejsie frontendu.
  </done>
</task>

</tasks>

<verification>
- `npm run build` — zero błędów
- /onboarding: pełny flow formularz -> chat -> sukces
- /profile: sekcja "Profil biznesowy" z formularzem edycji
- /api/curriculum/clarify: dodaje kontekst biznesowy do system promptu (jeśli profil istnieje)
- /api/curriculum/generate: dodaje kontekst biznesowy do user promptu (jeśli profil istnieje)
- Bez profilu: oba endpointy działają identycznie jak wcześniej
</verification>

<success_criteria>
- ONB-05: Edycja profilu biznesowego ze strony /profile działa
- ONB-06: Generowanie kursów uwzględnia profil (widoczne w pytaniach AI i strukturze kursu)
- Zero regresji: istniejące kursy bez profilu generują się normalnie
- Pełny flow onboardingu: formularz -> opcjonalny chat -> dashboard
</success_criteria>

<output>
After completion, create `.planning/phases/08-business-onboarding/08-04-SUMMARY.md`
</output>
</output>
