---
phase: 09-business-suggestions
plan: 03
type: execute
wave: 3
depends_on: ["09-02"]
files_modified:
  - src/components/business-ideas/InlineSuggestion.tsx
  - src/components/business-ideas/GenerateSuggestionButton.tsx
  - src/components/materials/content-renderer.tsx
  - src/app/(dashboard)/courses/[courseId]/[levelId]/[chapterId]/page.tsx
  - src/app/(dashboard)/courses/[courseId]/[levelId]/[chapterId]/components/chapter-page-client.tsx
autonomous: true

must_haves:
  truths:
    - "Uzytkownik widzi przycisk Pokaz pomysl biznesowy w headerze lekcji"
    - "Po kliknieciu przycisku sugestia pojawia sie inline pod odpowiednia sekcja h2"
    - "Uzytkownik moze zapisac (bookmark) lub ukryc (dismiss) sugestie"
    - "Licznik Pozostalo X/5 na dzis widoczny przy przycisku"
    - "Przycisk disabled gdy brak tresci lekcji lub wyczerpany limit"
    - "Callout uzupelnij profil widoczny pod sugestia gdy brak profilu"
    - "Ponowne otwarcie lekcji pokazuje cached sugestie bez wywolania AI"
  artifacts:
    - path: "src/components/business-ideas/InlineSuggestion.tsx"
      provides: "Karta sugestii inline przy h2 (compact variant)"
      exports: ["InlineSuggestion"]
    - path: "src/components/business-ideas/GenerateSuggestionButton.tsx"
      provides: "Przycisk generowania z licznikiem limitu"
      exports: ["GenerateSuggestionButton"]
  key_links:
    - from: "src/components/materials/content-renderer.tsx"
      to: "src/components/business-ideas/InlineSuggestion.tsx"
      via: "renderowanie pod dopasowanym h2"
      pattern: "InlineSuggestion"
    - from: "src/app/(dashboard)/courses/[courseId]/[levelId]/[chapterId]/page.tsx"
      to: "src/lib/business-ideas/ideas-dal.ts"
      via: "getSuggestion() w server component"
      pattern: "getSuggestion"
    - from: "src/components/business-ideas/GenerateSuggestionButton.tsx"
      to: "src/hooks/use-chapter-suggestion.ts"
      via: "generate callback z hooka"
      pattern: "generate|useChapterSuggestion"
---

<objective>
UI komponenty sugestii biznesowych + integracja z content-renderer i strona chapter.

Purpose: Dostarczyc kompletny UX: przycisk generowania w headerze, sugestia inline przy h2, bookmark/dismiss/refresh akcje.
Output: 2 nowe komponenty + integracja z istniejacym content-renderer + chapter page (server + client).
</objective>

<execution_context>
@/Users/dariuszciesielski/.claude/get-shit-done/workflows/execute-plan.md
@/Users/dariuszciesielski/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-business-suggestions/CONTEXT.md
@.planning/phases/09-business-suggestions/09-RESEARCH.md
@.planning/phases/09-business-suggestions/09-01-SUMMARY.md
@.planning/phases/09-business-suggestions/09-02-SUMMARY.md
@src/components/materials/content-renderer.tsx
@src/components/materials/section-image.tsx
@src/app/(dashboard)/courses/[courseId]/[levelId]/[chapterId]/page.tsx
@src/app/(dashboard)/courses/[courseId]/[levelId]/[chapterId]/components/chapter-page-client.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Komponenty UI — InlineSuggestion + GenerateSuggestionButton</name>
  <files>
    src/components/business-ideas/InlineSuggestion.tsx
    src/components/business-ideas/GenerateSuggestionButton.tsx
  </files>
  <action>
  1. Stworz `src/components/business-ideas/InlineSuggestion.tsx`:

  Komponent renderujacy karte sugestii inline (wariant compact domyslnie):

  - Uzyj shadcn/ui: Card, CardContent, CardHeader, CardTitle, Badge, Button, Tooltip
  - Ikony lucide-react: Lightbulb, Bookmark, BookmarkCheck, X, RefreshCw, ChevronDown, ChevronUp
  - Props:
    * `suggestion: BusinessSuggestion`
    * `variant?: 'compact' | 'hint'` (domyslnie 'compact')
    * `showRefresh?: boolean` — widoczny gdy profile_version zmieniony
    * `hasProfile?: boolean` — czy uzytkownik ma profil biznesowy
    * `onBookmark?: () => void`
    * `onDismiss?: () => void`
    * `onRefresh?: () => void`

  Layout compact (wariant A z CONTEXT.md):
  - Card z lewym borderem kolorowym (amber/yellow — biznes)
  - Header: ikona Lightbulb + tytul + Badge z estimated_complexity (prosty=green, sredni=yellow, zlozony=red)
  - Content: opis (description), rozwijalny "Potencjal biznesowy" (business_potential) z ChevronDown/Up
  - Footer: 3 przyciski — Zapisz (Bookmark toggle), Ukryj (X), Odswież (RefreshCw, tylko jesli showRefresh)
  - Animacja: fade-in (CSS transition opacity + transform)

  Layout hint (wariant C):
  - Prostsza wersja: maly hint-box, tooltip-like, bez rozwijalnej sekcji
  - Na MVP: wariant hint moze byc uproszczony — compact jest priorytetem

  Callout pod karta (gdy `!hasProfile`):
  - Lekki div z tekstem: "Uzupelnij profil biznesowy, zeby sugestie byly dopasowane do Twojej branzy"
  - Link do /onboarding
  - Styl: bg-muted, text-sm, rounded, padding, border-l-2 border-amber-500

  2. Stworz `src/components/business-ideas/GenerateSuggestionButton.tsx`:

  Przycisk generowania w headerze lekcji:

  - Props:
    * `isGenerating: boolean`
    * `remaining: number | null`
    * `hasContent: boolean` — czy lekcja ma wygenerowana tresc
    * `hasSuggestion: boolean` — czy juz jest sugestia (nie pokazuj przycisku?)
    * `onClick: () => void`

  Zachowanie:
  - Jesli `!hasContent` → disabled + Tooltip "Najpierw wygeneruj materialy"
  - Jesli `remaining === 0` → disabled + Tooltip "Dzisiejszy limit zostal wykorzystany"
  - Jesli `isGenerating` → disabled + Loader2 spinner + "Analizuje lekcje..."
  - Normalny: ikona Lightbulb + "Pokaz pomysl biznesowy"
  - Pod przyciskiem lub obok: maly tekst "Pozostalo X/5 na dzis" (jesli remaining !== null)
  - Styl: wariant outline lub secondary, nie za dominujacy (przycisk pomocniczy, nie glowny CTA)
  </action>
  <verify>
  - `npx tsc --noEmit` — brak bledow
  - Oba komponenty eksportuja sie poprawnie
  - InlineSuggestion renderuje Card z tytul, opis, badge, 3 przyciski
  - GenerateSuggestionButton ma stany: disabled (brak tresci), disabled (limit), loading, normalny
  </verify>
  <done>
  - InlineSuggestion: karta compact z bookmark/dismiss/refresh akcjami
  - InlineSuggestion: callout "uzupelnij profil" gdy brak profilu
  - GenerateSuggestionButton: przycisk z limitem, stanami disabled/loading
  - Polskie teksty z prawidlowymi diakrytykami
  </done>
</task>

<task type="auto">
  <name>Task 2: Integracja z content-renderer + chapter page</name>
  <files>
    src/components/materials/content-renderer.tsx
    src/app/(dashboard)/courses/[courseId]/[levelId]/[chapterId]/page.tsx
    src/app/(dashboard)/courses/[courseId]/[levelId]/[chapterId]/components/chapter-page-client.tsx
  </files>
  <action>
  1. Rozszerz `content-renderer.tsx`:

  Dodaj nowe propsy (analogicznie do images):
  - `suggestion?: BusinessSuggestion | null`
  - `suggestionLoading?: boolean`
  - `suggestionTarget?: string | null` — heading h2 gdzie pokazac skeleton

  W renderowaniu h2 (analogicznie do SectionImage):
  - Uzyj `findSectionSuggestion()` (nowa funkcja, wzorzec z findSectionImage):
    * Exact match suggestion.relevant_section === headingText
    * Fuzzy: stripHeadingNumber na obu stronach
  - Jesli dopasowano → renderuj `<InlineSuggestion>` pod h2 (po SectionImage jesli jest)
  - Jesli `suggestionLoading` i `suggestionTarget` pasuje do tego h2 → skeleton placeholder
  - Fallback: jesli suggestion istnieje ale zadne h2 nie pasuje → renderuj na poczatku tresci (przed pierwszym elementem)

  WAZNE: Content-renderer juz ma ~16 propsow. Dodaj TYLKO 3 nowe propsy (suggestion, suggestionLoading, suggestionTarget). Przycisk generowania jest w HEADERZE lekcji (chapter-page-client), NIE w content-renderer.

  2. Rozszerz `page.tsx` (server component):

  Dodaj import i wywolanie:
  ```typescript
  import { getSuggestion } from '@/lib/business-ideas/ideas-dal'
  import { getBusinessProfile } from '@/lib/onboarding/onboarding-dal'

  // W body funkcji, obok istniejacych fetchow:
  const [initialSuggestion, businessProfile] = await Promise.all([
    getSuggestion(chapterId),
    getBusinessProfile(),
  ])
  ```

  Przekaz `initialSuggestion` i `businessProfile` do `ChapterPageClient` jako propsy.

  3. Rozszerz `chapter-page-client.tsx`:

  - Dodaj `useChapterSuggestion(chapterId, initialSuggestion)` hook
  - Dodaj `GenerateSuggestionButton` w headerze lekcji (obok istniejacych metadanych/przyciskow)
  - Przekaz callback `generate` z hooka do onClick przycisku, z parametrami: courseId, content (z sectionContent), chapterTitle (chapter.title), courseTopic (course.topic)
  - Przekaz `suggestion` z hooka + `bookmark`/`dismiss`/`refresh` callbacki do ContentRenderer
  - Przekaz `hasProfile: !!businessProfile` do InlineSuggestion (przez content-renderer)
  - `showRefresh`: porownaj suggestion.profile_version z businessProfile.profile_version — jesli suggestion < aktualny → pokaz przycisk Odswież
  - `suggestionTarget`: gdy isGenerating, wyekstrahuj headingi h2 z content i uzyj pierwszego jako target skeleton (lub null jesli jeszcze nie wiadomo)

  WAZNE:
  - NIE dodawaj onGenerateSuggestion do content-renderer — przycisk jest w headerze
  - Sugestia jest 1 per chapter (nie per section)
  - Reuse `canAccessPremiumFeature` jesli sugestie maja byc premium (sprawdz CONTEXT.md — NIE, sugestie sa dla wszystkich)
  </action>
  <verify>
  - `npx tsc --noEmit` — brak bledow
  - `npm run build` — brak bledow build
  - Otworz lekcje z wygenerowana trescia → przycisk "Pokaz pomysl biznesowy" widoczny w headerze
  - Kliknij przycisk → sugestia pojawia sie pod odpowiednia sekcja h2
  - Odswież strone → cached sugestia wyswietla sie od razu (bez AI call)
  - Bookmark → ikona sie zmienia, toast "Pomysl zapisany"
  - Dismiss → karta znika, toast "Pomysl ukryty"
  </verify>
  <done>
  - Przycisk "Pokaz pomysl biznesowy" w headerze lekcji z licznikiem limitu
  - Sugestia renderuje sie inline pod dopasowana sekcja h2
  - Fuzzy heading match dziala (z numerami i bez)
  - Fallback na poczatek lekcji gdy h2 nie pasuje
  - Cached sugestie laduja z server component (brak AI call przy refresh)
  - Bookmark/dismiss/refresh dzialaja z optimistic updates
  - Callout "uzupelnij profil" widoczny gdy brak profilu
  </done>
</task>

</tasks>

<verification>
- `npm run build` przechodzi bez bledow
- Pelny flow: otworz lekcje → kliknij "Pokaz pomysl" → sugestia inline → bookmark → dismiss
- Cache: zamknij i otworz lekcje ponownie → sugestia zaladowana z DB
- Limit: po 5 generowaniach przycisk disabled
- Bez profilu: sugestia generuje sie (ogolna) + callout "uzupelnij profil"
</verification>

<success_criteria>
- Kompletny UX sugestii biznesowych dziala end-to-end
- Sugestia wyswietla sie inline przy wlasciwej sekcji h2
- Cache, rate limit, bookmark, dismiss, refresh dzialaja poprawnie
- UI reaguje na stan: loading, limit exceeded, brak tresci
- Polskie teksty z prawidlowymi diakrytykami
</success_criteria>

<output>
After completion, create `.planning/phases/09-business-suggestions/09-03-SUMMARY.md`
</output>
