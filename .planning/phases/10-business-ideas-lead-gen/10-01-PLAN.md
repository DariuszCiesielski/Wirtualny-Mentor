---
phase: 10-business-ideas-lead-gen
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/types/business-ideas.ts
  - src/lib/business-ideas/ideas-dal.ts
  - src/components/layout/sidebar.tsx
  - src/components/layout/mobile-nav.tsx
  - src/components/business-ideas/ContactCTA.tsx
autonomous: true

must_haves:
  truths:
    - "Typy BookmarkedSuggestionWithContext i ContactInfo istnieja i sa eksportowane"
    - "DAL zwraca bookmarkowane sugestie z kontekstem kursu i rozdzialu (tytuly, level_id)"
    - "DAL zwraca liste kursow z bookmarkowanymi sugestiami (do filtra)"
    - "Sidebar i MobileNav pokazuja 'Pomysly biznesowe' z ikona Lightbulb po Notatki a przed Profil"
    - "ContactCTA renderuje dane kontaktowe z props lub null gdy brak danych"
  artifacts:
    - path: "src/types/business-ideas.ts"
      provides: "BookmarkedSuggestionWithContext, ContactInfo types"
      contains: "BookmarkedSuggestionWithContext"
    - path: "src/lib/business-ideas/ideas-dal.ts"
      provides: "getBookmarkedSuggestions, getCoursesWithBookmarks"
      contains: "getBookmarkedSuggestions"
    - path: "src/components/layout/sidebar.tsx"
      provides: "Nav item Pomysly biznesowe"
      contains: "Lightbulb"
    - path: "src/components/layout/mobile-nav.tsx"
      provides: "Nav item Pomysly biznesowe (mobile)"
      contains: "Lightbulb"
    - path: "src/components/business-ideas/ContactCTA.tsx"
      provides: "Contact CTA component"
      contains: "ContactCTA"
  key_links:
    - from: "src/lib/business-ideas/ideas-dal.ts"
      to: "business_suggestions"
      via: "Supabase PostgREST JOIN on courses, chapters"
      pattern: "courses.*title.*chapters.*title.*level_id"
---

<objective>
Przygotowac fundament fazy 10: typy, funkcje DAL, nawigacje w sidebar/mobile i komponent ContactCTA.

Purpose: Dostarcza dane i nawigacje potrzebne do zbudowania strony /business-ideas w planie 02.
Output: Rozszerzone typy, 2 nowe funkcje DAL, zaktualizowana nawigacja, komponent CTA.
</objective>

<execution_context>
@/Users/dariuszciesielski/.claude/get-shit-done/workflows/execute-plan.md
@/Users/dariuszciesielski/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-business-ideas-lead-gen/10-CONTEXT.md
@.planning/phases/10-business-ideas-lead-gen/10-RESEARCH.md
@src/types/business-ideas.ts
@src/lib/business-ideas/ideas-dal.ts
@src/components/layout/sidebar.tsx
@src/components/layout/mobile-nav.tsx
@src/components/business-ideas/InlineSuggestion.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Typy + DAL (getBookmarkedSuggestions, getCoursesWithBookmarks)</name>
  <files>src/types/business-ideas.ts, src/lib/business-ideas/ideas-dal.ts</files>
  <action>
1. W `src/types/business-ideas.ts` DODAJ (nie usuwaj istniejacych typow):

```typescript
export interface BookmarkedSuggestionWithContext {
  id: string;
  title: string;
  description: string;
  business_potential: string;
  estimated_complexity: "prosty" | "sredni" | "zlozony";
  relevant_section: string | null;
  course_id: string;
  chapter_id: string;
  created_at: string;
  course_title: string;
  chapter_title: string;
  level_id: string;
}

export interface ContactInfo {
  email: string | null;
  phone: string | null;
  formUrl: string | null;
}
```

2. W `src/lib/business-ideas/ideas-dal.ts` DODAJ dwie nowe funkcje na koncu pliku (nie modyfikuj istniejacych):

- `getBookmarkedSuggestions(userId: string, courseId?: string): Promise<BookmarkedSuggestionWithContext[]>` — query `business_suggestions` z `select("id, title, description, business_potential, estimated_complexity, relevant_section, course_id, chapter_id, created_at, courses(title), chapters(title, level_id)")`, filtruj `user_id`, `is_bookmarked=true`, `is_dismissed=false`, opcjonalny `course_id`, `order("created_at", { ascending: false })`. Mapuj wynik: `course_title` z `row.courses.title`, `chapter_title` z `row.chapters.title`, `level_id` z `row.chapters.level_id`. Auth check: `supabase.auth.getUser()`, return `[]` jesli brak usera lub userId nie pasuje.

- `getCoursesWithBookmarks(userId: string): Promise<{ id: string; title: string }[]>` — query `business_suggestions` z `select("course_id, courses(id, title)")`, filtruj `user_id`, `is_bookmarked=true`, `is_dismissed=false`. Deduplikuj po `course_id` (Set). Auth check jak wyzej.

Dodaj import `BookmarkedSuggestionWithContext` z types.

WAZNE: PostgREST JOIN syntax — uzyj `courses ( title )` i `chapters ( title, level_id )` (ze spacjami w nawiasach). Rzutuj `row.courses` jako `{ title: string }` i `row.chapters` jako `{ title: string; level_id: string }`.
  </action>
  <verify>
`npm run build` przechodzi bez bledow TypeScript. Grep potwierdza eksport obu funkcji DAL i obu nowych typow.
  </verify>
  <done>
Typy BookmarkedSuggestionWithContext i ContactInfo sa eksportowane. Funkcje getBookmarkedSuggestions i getCoursesWithBookmarks sa zaimplementowane z poprawnymi JOINami i auth checkiem.
  </done>
</task>

<task type="auto">
  <name>Task 2: Nawigacja sidebar + mobile-nav + ContactCTA</name>
  <files>src/components/layout/sidebar.tsx, src/components/layout/mobile-nav.tsx, src/components/business-ideas/ContactCTA.tsx</files>
  <action>
1. W `src/components/layout/sidebar.tsx`:
   - Dodaj `Lightbulb` do importu z `lucide-react` (linia 12)
   - W tablicy `navItems` (linia 16-41) wstaw NOWY element PO "Notatki" (index 2) a PRZED "Profil" (index 3):
     ```
     { label: "Pomysly biznesowe", href: "/business-ideas", icon: Lightbulb, disabled: false },
     ```
   - Polskie znaki w labelu: "Pomysly biznesowe" (z akcentami: Pomysly -> Pomysly, tu nie ma polskich znakow do dodania, ale label jest poprawny)
   - UWAGA: label to "Pomysly biznesowe" — bez polskich znakow bo 'l' i 'y' sa ASCII. Ale... sprawdz: "Pomysly" powinno byc "Pomysly". OK, nie ma tu polskich diakrytykow.

   Poprawka: Label powinien byc "Pomysly biznesowe" — w polskim: "Pomysly" nie wymaga diakrytykow (nie ma tu a,e,c,l,n,o,s,z,z). Ale dokladny tekst z CONTEXT.md to "Pomysly biznesowe" — uzyj dokladnie tego.

2. W `src/components/layout/mobile-nav.tsx`:
   - Dokladnie te same zmiany co w sidebar.tsx:
   - Dodaj `Lightbulb` do importu z `lucide-react` (linia 14)
   - W tablicy `navItems` (linia 25-50) wstaw ten sam element po "Notatki" przed "Profil"

3. Utworz `src/components/business-ideas/ContactCTA.tsx`:

```typescript
// Client component
'use client';

import { Button } from '@/components/ui/button';
import { ExternalLink, Mail, Phone } from 'lucide-react';
import type { ContactInfo } from '@/types/business-ideas';

interface ContactCTAProps {
  contactInfo: ContactInfo;
}

export function ContactCTA({ contactInfo }: ContactCTAProps) {
  if (!contactInfo.email && !contactInfo.phone && !contactInfo.formUrl) {
    return null;
  }

  const primaryAction = contactInfo.formUrl
    ? { href: contactInfo.formUrl, label: 'Napisz wiadomosc', icon: ExternalLink, target: '_blank' as const }
    : contactInfo.email
    ? { href: `mailto:${contactInfo.email}`, label: 'Napisz wiadomosc', icon: Mail, target: undefined }
    : null;

  return (
    <div className="mt-4 rounded-lg border bg-muted/50 p-4 space-y-3">
      <h4 className="font-medium text-sm">
        Chcesz sprawdzic, czy ten pomysl ma sens w Twoim przypadku?
      </h4>
      <p className="text-sm text-muted-foreground">
        Mozemy omowic zakres, wykonalnosc i prosta wersje startowa.
      </p>
      <div className="flex flex-col sm:flex-row gap-2">
        {primaryAction && (
          <Button asChild size="sm">
            <a href={primaryAction.href} target={primaryAction.target} rel={primaryAction.target ? 'noopener noreferrer' : undefined}>
              <primaryAction.icon className="h-4 w-4 mr-2" />
              {primaryAction.label}
            </a>
          </Button>
        )}
        {contactInfo.phone && (
          <Button variant="outline" size="sm" asChild>
            <a href={`tel:${contactInfo.phone}`}>
              <Phone className="h-4 w-4 mr-2" />
              {contactInfo.phone}
            </a>
          </Button>
        )}
      </div>
      <p className="text-xs text-muted-foreground italic">
        To inspiracja, nie gotowa rekomendacja biznesowa.
      </p>
    </div>
  );
}
```

KRYTYCZNE: Wszystkie polskie teksty MUSZA miec polskie diakrytyki:
- "Napisz wiadomosc" -> "Napisz wiadomosc" (tu akurat nie ma diakrytykow, poprawne)
- Hmm, sprawdzmy: "wiadomosc" -> "wiadomosc" -> poprawnie to "wiadomość" (z s-ogonek!)
- "sprawdzic" -> "sprawdzić"
- "pomysl" -> "pomysł"
- "Mozemy" -> "Możemy"
- "omowic" -> "omówić"
- "wykonalnosc" -> "wykonalność"
- "prosta" -> "prostą"
- "wersje" -> "wersję"
- "startowa" -> "startową"

Uzyj DOKLADNYCH tekstow z CONTEXT.md (ktory ma poprawne polskie znaki):
- Naglowek: "Chcesz sprawdzic, czy ten pomysl ma sens w Twoim przypadku?" (z diakrytykami!)
- Opis: "Mozemy omowic zakres, wykonalnosc i prosta wersje startowa." (z diakrytykami!)
- Przycisk: "Napisz wiadomosc" (z diakrytykami!)
- Disclaimer: "To inspiracja, nie gotowa rekomendacja biznesowa."

Poprawne wersje (SKOPIUJ Z CONTEXT.md):
- "Chcesz sprawdzic, czy ten pomysl ma sens w Twoim przypadku?" z CONTEXT = "Chcesz sprawdzic, czy ten pomysl ma sens w Twoim przypadku?"

OK — najlepiej skopiuj teksty DOKLADNIE z sekcji 2 pliku 10-CONTEXT.md. Tam sa poprawne polskie znaki.
  </action>
  <verify>
`npm run build` przechodzi. Sidebar i MobileNav renderuja "Pomysly biznesowe" z Lightbulb. Grep potwierdza Lightbulb w obu plikach nawigacji. ContactCTA.tsx eksportuje komponent.
  </verify>
  <done>
Nawigacja w sidebar i mobile-nav zawiera "Pomysly biznesowe" z ikona Lightbulb na pozycji po Notatki, przed Profil. ContactCTA renderuje dane kontaktowe z graceful degradation i mini-disclaimer.
  </done>
</task>

</tasks>

<verification>
- `npm run build` przechodzi bez bledow
- `grep -r "getBookmarkedSuggestions" src/lib/business-ideas/ideas-dal.ts` — funkcja istnieje
- `grep -r "getCoursesWithBookmarks" src/lib/business-ideas/ideas-dal.ts` — funkcja istnieje
- `grep -r "BookmarkedSuggestionWithContext" src/types/business-ideas.ts` — typ istnieje
- `grep -r "ContactInfo" src/types/business-ideas.ts` — typ istnieje
- `grep -r "Lightbulb" src/components/layout/sidebar.tsx src/components/layout/mobile-nav.tsx` — w obu plikach
- `grep -r "business-ideas" src/components/layout/sidebar.tsx src/components/layout/mobile-nav.tsx` — href w obu
- `grep -r "ContactCTA" src/components/business-ideas/ContactCTA.tsx` — komponent istnieje
</verification>

<success_criteria>
1. Typy BookmarkedSuggestionWithContext i ContactInfo eksportowane z business-ideas.ts
2. DAL ma getBookmarkedSuggestions (z JOIN na courses/chapters) i getCoursesWithBookmarks
3. Sidebar i MobileNav maja pozycje "Pomysly biznesowe" z Lightbulb po Notatki
4. ContactCTA renderuje CTA z graceful degradation (null gdy brak danych kontaktowych)
5. Build przechodzi bez bledow
</success_criteria>

<output>
After completion, create `.planning/phases/10-business-ideas-lead-gen/10-01-SUMMARY.md`
</output>
