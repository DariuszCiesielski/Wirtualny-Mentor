---
phase: 10-business-ideas-lead-gen
plan: 02
type: execute
wave: 2
depends_on: ["10-01"]
files_modified:
  - src/app/(dashboard)/business-ideas/page.tsx
  - src/components/business-ideas/BusinessIdeasClient.tsx
  - src/components/business-ideas/IdeaCard.tsx
autonomous: true

must_haves:
  truths:
    - "Uzytkownik widzi strone /business-ideas z lista bookmarkowanych sugestii"
    - "Uzytkownik moze filtrowac pomysly po kursie z dropdown"
    - "Uzytkownik moze sortowac po dacie lub zlozonosci"
    - "Pierwsze 3 karty sa rozwiniete domyslnie, reszta zwinieta"
    - "Karta zwinieta pokazuje tytul, 2-3 linie opisu, badge zlozonosci, zrodlo, akcje"
    - "Karta rozwinieta pokazuje pelny opis, potencjal biznesowy, CTA kontaktowe"
    - "Uzytkownik moze usunac bookmark (optimistic update)"
    - "Strona ma disclaimer informacyjny na gorze"
    - "3 osobne puste stany: brak kursow, brak bookmarkow, filtr bez wynikow"
    - "CTA kontaktowe widoczne w rozwinietej karcie z danymi z ENV"
  artifacts:
    - path: "src/app/(dashboard)/business-ideas/page.tsx"
      provides: "Server page with auth, DAL calls, ENV reading"
      contains: "requireAuth"
    - path: "src/components/business-ideas/BusinessIdeasClient.tsx"
      provides: "Client wrapper with filter, sort, expand state"
      contains: "BusinessIdeasClient"
    - path: "src/components/business-ideas/IdeaCard.tsx"
      provides: "Expand/collapse card for bookmarked suggestion"
      contains: "IdeaCard"
  key_links:
    - from: "src/app/(dashboard)/business-ideas/page.tsx"
      to: "src/lib/business-ideas/ideas-dal.ts"
      via: "getBookmarkedSuggestions + getCoursesWithBookmarks"
      pattern: "getBookmarkedSuggestions|getCoursesWithBookmarks"
    - from: "src/app/(dashboard)/business-ideas/page.tsx"
      to: "process.env.CONTACT_"
      via: "Server-side ENV reading, passed as props"
      pattern: "process\\.env\\.CONTACT_"
    - from: "src/components/business-ideas/BusinessIdeasClient.tsx"
      to: "src/components/business-ideas/IdeaCard.tsx"
      via: "Renders IdeaCard for each suggestion"
      pattern: "IdeaCard"
    - from: "src/components/business-ideas/IdeaCard.tsx"
      to: "src/components/business-ideas/ContactCTA.tsx"
      via: "Renders ContactCTA in expanded state"
      pattern: "ContactCTA"
    - from: "src/components/business-ideas/IdeaCard.tsx"
      to: "src/lib/business-ideas/ideas-dal.ts"
      via: "bookmarkSuggestion for un-bookmark"
      pattern: "bookmarkSuggestion"
---

<objective>
Zbudowac strone /business-ideas z pelna funkcjonalnoscia: server page, filtrowanie, sortowanie, expand/collapse, CTA kontaktowe, disclaimer, puste stany, optimistic un-bookmark.

Purpose: Koncowy element fazy 10 — uzytkownik ma jedno miejsce do przegladania zapisanych pomyslow biznesowych z mozliwoscia kontaktu.
Output: Kompletna strona /business-ideas gotowa do uzycia.
</objective>

<execution_context>
@/Users/dariuszciesielski/.claude/get-shit-done/workflows/execute-plan.md
@/Users/dariuszciesielski/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-business-ideas-lead-gen/10-CONTEXT.md
@.planning/phases/10-business-ideas-lead-gen/10-RESEARCH.md
@.planning/phases/10-business-ideas-lead-gen/10-01-SUMMARY.md
@src/types/business-ideas.ts
@src/lib/business-ideas/ideas-dal.ts
@src/components/business-ideas/InlineSuggestion.tsx
@src/components/business-ideas/ContactCTA.tsx
@src/lib/dal/auth.ts
@src/app/(dashboard)/notes/page.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Server page + BusinessIdeasClient (filtrowanie, sortowanie, puste stany)</name>
  <files>src/app/(dashboard)/business-ideas/page.tsx, src/components/business-ideas/BusinessIdeasClient.tsx</files>
  <action>
1. Utworz `src/app/(dashboard)/business-ideas/page.tsx` (server component):

```typescript
import { requireAuth } from '@/lib/dal/auth';
import { getBookmarkedSuggestions, getCoursesWithBookmarks } from '@/lib/business-ideas/ideas-dal';
import { ContentContainer } from '@/components/layout/content-container';
import { BusinessIdeasClient } from '@/components/business-ideas/BusinessIdeasClient';
import type { ContactInfo } from '@/types/business-ideas';

export default async function BusinessIdeasPage() {
  const user = await requireAuth();

  const [suggestions, courses] = await Promise.all([
    getBookmarkedSuggestions(user.id),
    getCoursesWithBookmarks(user.id),
  ]);

  // ENV - server only (no NEXT_PUBLIC_ prefix)
  const contactInfo: ContactInfo = {
    email: process.env.CONTACT_EMAIL || null,
    phone: process.env.CONTACT_PHONE || null,
    formUrl: process.env.CONTACT_FORM_URL || null,
  };
  const hasContact = !!(contactInfo.email || contactInfo.phone || contactInfo.formUrl);

  return (
    <ContentContainer className="py-8">
      <BusinessIdeasClient
        suggestions={suggestions}
        courses={courses}
        contactInfo={hasContact ? contactInfo : null}
      />
    </ContentContainer>
  );
}
```

2. Utworz `src/components/business-ideas/BusinessIdeasClient.tsx` ('use client'):

Props:
- `suggestions: BookmarkedSuggestionWithContext[]`
- `courses: { id: string; title: string }[]`
- `contactInfo: ContactInfo | null`

Stan:
- `items` — lokalna kopia suggestions (do optimistic update przy un-bookmark)
- `selectedCourseId: string | "all"` — filtr po kursie, domyslnie "all"
- `sortBy: "date" | "complexity"` — sortowanie, domyslnie "date"
- `expandedIds: Set<string>` — inicjalizowane z pierwszymi 3 ID z przefiltrowanych wynikow

Logika filtrowania i sortowania (useMemo):
- Filtruj `items` po `course_id` (jesli nie "all")
- Sortuj: "date" = `created_at` desc, "complexity" = prosty < sredni < zlozony
- Wynik: `filteredSuggestions`

Reset expandedIds przy zmianie filtra (useEffect na selectedCourseId):
- Reset do Set z pierwszych 3 ID z `filteredSuggestions`

Funkcja `handleRemoveBookmark(id: string)`:
- Optimistic: usun z `items` natychmiast
- W tle: wywolaj `bookmarkSuggestion(id)` (server action z ideas-dal.ts)
- Jesli fail: przywroc item (toast error z sonner)

Layout:
- Naglowek: h1 "Pomysly biznesowe" (z polskimi znakami!)
- Disclaimer Alert (shadcn Alert + AlertTitle + AlertDescription + ikona Info):
  "Pomysly na tej stronie maja charakter inspiracyjny i edukacyjny. Przed wdrozeniem ocen ich wykonalnosc, koszty i dopasowanie do swojej firmy."
- Filtry: flex row z:
  - Select "Wszystkie kursy" + opcje kursow (z `courses` prop)
  - Select sortowanie: "Ostatnio zapisane" / "Zlozonosc"
  - Licznik: Badge z "{N} pomyslow/pomysl/pomysly" (odmiana polska)
- Lista kart: mapuj `filteredSuggestions` na `<IdeaCard>` z props:
  - suggestion, isExpanded, onToggleExpand, onRemoveBookmark, contactInfo

3 PUSTE STANY (wzorzec z notes/page.tsx — Card z ikona, h3, p, Button):
- Brak kursow (`courses.length === 0`): Lightbulb icon, "Nie masz jeszcze kursow...", Button "Utworz pierwszy kurs" -> /courses/new
- Brak bookmarkow (`items.length === 0 && courses.length > 0`): Lightbulb icon, "Nie masz jeszcze zapisanych pomyslow...", Button "Przejdz do kursow" -> /courses
- Filtr bez wynikow (`filteredSuggestions.length === 0 && items.length > 0`): "Brak zapisanych pomyslow dla wybranego kursu.", link "Wyczysc filtr" (resetuje selectedCourseId na "all")

WAZNE — polskie diakrytyki we WSZYSTKICH tekstach UI (skopiuj z 10-CONTEXT.md):
- "Pomysly biznesowe" -> sprawdz: "Pomysly" jest poprawne (bez diakrytykow), "biznesowe" tez
- "Nie masz jeszcze kursow, wiec nie ma z czego tworzyc pomyslow biznesowych."
- Z CONTEXT.md sekcja 3 — uzyj DOKLADNYCH tekstow

Importy: shadcn Select, SelectContent, SelectItem, SelectTrigger, SelectValue; Alert, AlertTitle, AlertDescription; Card, CardContent; Badge; Button; Link; Lightbulb, Info z lucide-react; toast z sonner.
  </action>
  <verify>
`npm run build` przechodzi. Strona /business-ideas renderuje sie (nawet z pustymi danymi). Grep potwierdza BusinessIdeasClient z Select, Alert, IdeaCard.
  </verify>
  <done>
Server page czyta auth + DAL + ENV i przekazuje do BusinessIdeasClient. Client obsluguje filtrowanie po kursie, sortowanie, 3 puste stany, disclaimer Alert, optimistic un-bookmark.
  </done>
</task>

<task type="auto">
  <name>Task 2: IdeaCard (expand/collapse z CTA i zrodlem)</name>
  <files>src/components/business-ideas/IdeaCard.tsx</files>
  <action>
Utworz `src/components/business-ideas/IdeaCard.tsx` ('use client'):

Props:
- `suggestion: BookmarkedSuggestionWithContext`
- `isExpanded: boolean`
- `onToggleExpand: () => void`
- `onRemoveBookmark: () => void`
- `contactInfo: ContactInfo | null`

Wizualnie spojny z InlineSuggestion (ten sam styl karty):
- Card z `border-l-4 border-l-amber-500 bg-card/50`
- Lightbulb icon amber-500 przy tytule
- Badge zlozonosci z complexityConfig (reuse tego samego mapowania co w InlineSuggestion: prosty=green, sredni=yellow, zlozony=red)

STAN ZWINIETY (isExpanded=false):
- Tytul z Lightbulb + Badge zlozonosci (flex justify-between)
- Opis: line-clamp-2 (obciecie do 2-3 linii)
- Zrodlo: mala etykieta pod opisem: "z kursu: {course_title} · Rozdzial: {chapter_title}" — klikalne linki
- Akcje: 3 buttony w rzedzie:
  - "Zobacz szczegoly" (ChevronDown) -> onToggleExpand
  - "Przejdz do lekcji" (ExternalLink) -> Link do `/courses/${suggestion.course_id}/${suggestion.level_id}/${suggestion.chapter_id}`
  - "Usun z zapisanych" (Trash2) -> onRemoveBookmark
- Na dole zwiniętej karty: jesli contactInfo !== null, maly link "Chcesz to omowic?" ktory wywoluje onToggleExpand

STAN ROZWINIETY (isExpanded=true):
- Tytul z Lightbulb + Badge zlozonosci (bez zmian)
- Pelny opis (bez line-clamp)
- Zrodlo (bez zmian)
- Sekcja "Potencjal biznesowy": naglowek + pelny tekst `suggestion.business_potential`
- ContactCTA komponent (jesli contactInfo !== null)
- Akcje: te same 3 buttony ale "Zobacz szczegoly" -> "Zwin" (ChevronUp) + onToggleExpand

URL lekcji: `/courses/${suggestion.course_id}/${suggestion.level_id}/${suggestion.chapter_id}`

Responsive: mobile full-width, akcje flex-wrap z gap-2.

POLSKIE DIAKRYTYKI we wszystkich labelach:
- "Zobacz szczegoly" -> "Zobacz szczegoly" (sprawdz: "szczegoly" -> "szczegoly" -> poprawnie "szczegoly" nie ma diakrytykow... ale czekaj: "szczegoly" = "szczegóły"!)
- Poprawne: "Zobacz szczegóły", "Przejdź do lekcji", "Usuń z zapisanych", "Zwiń", "Chcesz to omówić?"
- "Potencjał biznesowy" (naglowek sekcji)
- Zrodlo: "z kursu:" i "Rozdział:"

Importy: Card, CardContent, CardHeader, CardTitle z shadcn; Badge; Button; Link z next/link; Lightbulb, ChevronDown, ChevronUp, ExternalLink, Trash2 z lucide-react; ContactCTA z ./ContactCTA; BookmarkedSuggestionWithContext, ContactInfo z types.
  </action>
  <verify>
`npm run build` przechodzi. IdeaCard renderuje sie z expand/collapse. Grep potwierdza import ContactCTA, Link do lekcji z level_id, complexityConfig.
  </verify>
  <done>
IdeaCard wyswietla karte pomyslu biznesowego z expand/collapse. Stan zwiniety: tytul, opis (line-clamp), badge, zrodlo, akcje, link "Chcesz to omowic?". Stan rozwiniety: pelny opis, potencjal biznesowy, CTA kontaktowe. Link do lekcji budowany z course_id/level_id/chapter_id.
  </done>
</task>

</tasks>

<verification>
- `npm run build` przechodzi bez bledow
- Strona /business-ideas renderuje sie po zalogowaniu
- Filtrowanie po kursie dziala (dropdown z kursami)
- Sortowanie po dacie i zlozonosci dziala
- Karty expand/collapse — pierwsze 3 rozwiniete, reszta zwinieta
- CTA kontaktowe widoczne w rozwiniętej karcie (gdy ENV ustawione)
- Disclaimer Alert na gorze strony
- Puste stany: brak kursow, brak bookmarkow, filtr bez wynikow
- Link "Przejdz do lekcji" prowadzi do poprawnego URL
- Un-bookmark optimistic update dziala (karta znika natychmiast)
- Mobile: karty full-width, CTA stacked vertically
</verification>

<success_criteria>
1. Strona /business-ideas renderuje liste bookmarkowanych sugestii z filtrowaniem i sortowaniem
2. IdeaCard ma expand/collapse z pelnym opisem, potencjalem biznesowym i CTA
3. Pierwsze 3 karty rozwiniete domyslnie
4. Disclaimer Alert na gorze strony
5. 3 osobne puste stany dzialaja poprawnie
6. Un-bookmark z optimistic update
7. Link do lekcji poprawny (z level_id)
8. CTA z danymi z ENV (graceful degradation)
9. Build przechodzi bez bledow
</success_criteria>

<output>
After completion, create `.planning/phases/10-business-ideas-lead-gen/10-02-SUMMARY.md`
</output>
